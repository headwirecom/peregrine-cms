import {IconLib} from '../../../../../../../../js/constants'

export default (vm) => {
  return {
    label: 'insert image',
    icon: 'picture-o',
    iconLib: IconLib.FONT_AWESOME,
    cmd: 'insertImage'
  }
}