module.exports = {
    convert: function($, f) {
        //f.bindPath($)
        //f.addFor($.find('li.children').first(), 'model.childrenPages', 'child')
        //f.bindAttribute($.find('a').first(), 'href', f.pathToUrl('child.path'))
        //f.mapField($.find('a').first(), 'child.title')
        //f.replace( $.find('ul.nested').eq(0), '<themeclean-components-pagelistnested v-bind:model="model"></themeclean-components-pagelistnested>')
    }
}